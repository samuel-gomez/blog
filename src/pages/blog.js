import React from "react"
import Layout from "../templates/Layout"
import Posts from "../components/Posts"

const Blog = props => {
  return (
    <Layout>
      <Posts />
    </Layout>
  )
}

export default Blog
